# 移动端视觉预设 R2 验收

日期：2026-09-25。范围：代表页视觉样板及 skill 路由，不是三页完整原型或上线验收。

## 浏览器证据

- 新三稿使用相同的商品、字段和操作位置；390×844 同题彩色、灰度和筛选面板并排自查。字排、容器、图片位置、控件与价格分格均有区别；这是 Agent 的视觉自查，未据此推断用户的风格选择。
- 320×740、360×800、390×844，共 9 组正常尺寸：页面横向溢出 0，可见独立按钮均 >=44×44，输入 16px，商品状态 14px。
- 390 与 320 宽度下，三稿 CSS 文字倍率设为 200%（状态文字实际为 28px）：文档横溢 0，商品名称、编码、规格、金额区域未测得内部横溢；实际滚动检查 C 的文字和价格完整可达。没有把这个方法称为浏览器缩放、系统动态字体或真机验证。
- 200% 放大且窄屏下应用“针织 + 秋季新品”，结果保留完整条件与清除入口，0 横溢。结果区通过自然换行消除第一轮单行挤压。
- 三稿分别输入 DN260903，均只命中牛仔裤；不存在的关键字显示空结果，清除恢复 8 款；未应用的分类变更在关闭面板后不生效。
- 组合筛选“针织 + 秋季新品”得到细羊毛圆领针织衫；草稿页签得到 1 款且保留键盘焦点。
- 320 宽度打开最后一件商品、Esc 关闭后：滚动位置仍为 1344.5，焦点回到对应记录；末项底边 583.6、导航顶边 673，最后一条记录可达。
- 单屏交互页控制台读取结果：Errors 0 / Warnings 0。并排页的工具会话另捕获 1 条无来源 URL 的 `MutationObserver.observe` 异常；样板代码无该调用、无外部脚本，具体来源未确认。三框渲染、灰度切换与筛选操作均已实际完成，不把该页写成零错误。
- 原始尺寸与交互结果见 [layout-check.json](layout-check.json)；截图在 screenshots/。

## Skill 与实现检查

- skill 官方 quick_validate 通过；JavaScript 语法与 git diff --check 通过。前者只证明格式，不证明设计质量。
- 独立前向试跑：以新项目“社区活动列表、纸本编辑感、只要简报”为输入，正确转为活动日期/报名状态、未强制三选一、未复制商品字段、未添加底部导航或新页面。该结果仅证明这个案例的指令执行表现。
- 第一轮审查加入自然换行和文字倍率检查，修复放大时结果摘要与清除操作的挤压；第二轮相关路径复验通过。

## 未验证

真实 iOS/Android 字体、软键盘、触控误触、屏幕阅读器、安全区硬件、系统返回手势、微信或原生宿主，以及真实数据与接口未验证。字体使用系统字体栈，不同设备可能有不同的回退效果。

首页、完整商品详情和完整三页任务链尚未设计；用户选定视觉方向后继续。当前样板不代表业务接入或发布完成。2026-09-25 用户认可本轮整体结果并同意整理发布 skill，未选定单一风格用于三页扩展。
