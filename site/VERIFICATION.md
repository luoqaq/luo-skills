# 可发现性与介绍页验证

2026-09-26。本轮为本地文件与浏览器验收，尚未提交、推送或部署，不代表搜索引擎或技能目录已收录。

## 已验证

- Skill 官方 `quick_validate.py` 通过；使用本机已有 PyYAML，未新增项目依赖。`agents/openai.yaml` 可解析，简介长度与示例调用名称符合规范。
- JavaScript 语法、`git diff --check` 通过。独立只读审查未发现需修复问题。
- 预览与正式网址两种构建通过：15 个发布文件、23 项本地资源引用；预览保留 `noindex`，正式构建的 canonical、OG 图片、JSON-LD 与子路径 sitemap 一致。正式网址只用于离线构建验证，未部署。
- 6 个演示 HTML 均保留 `noindex,follow`。输出未包含个人记忆、仓库配置或案例研究文档。非空目录拒绝覆盖，已有文件保持不变。
- Chromium 实际检查 1440×1000、390×844、320×740：介绍页无横向溢出，主图加载成功；动画结束后完成桌面和手机截图自查。
- 安装命令与任务示例实际复制成功；剪贴板失败时选中文字并显示手动复制提示。禁用 JavaScript 后正文和链接仍可读，复制按钮隐藏。
- 实际点击手机演示入口、切换「机能终端」、搜索 `DN260903`；并排页三个嵌入页面完成加载；PC 演示切换「现代账本」成功。
- 上述浏览器路径：Errors 0 / Warnings 0 / HTTP 4xx–5xx 0。这仅覆盖本轮访问的本地资源及操作。

## 待验证

- GitHub 上新文档及元信息的实际呈现，正式部署 URL、canonical、分享预览与 sitemap 的线上访问。
- 搜索引擎收录、skills.sh 目录收录及自然搜索曝光，不能由本地检查推断。
- 真机、原生宿主和未执行的演示业务路径。现有样板的历史验收记录保留在各自目录中。

## 首页 PC 入口补充验证

2026-09-26：新增现有 PC 研究刊物样板的真实截图，首页与移动端样板并列展示，并提供单独的 PC / 移动端入口；未新增案例。

- 最新构建为 16 个文件、24 项本地资源引用检查通过。
- 1440×900 两张预览图均在首屏内完整显示；390×844、320×740 按 PC、移动端顺序排列，无横向溢出，图片全部加载。
- 点击 PC 预览进入方向切换页，实际持仓表格完成加载；移动端预览进入三框并排页。
- 最新访问路径浏览器脚本错误及 HTTP 4xx–5xx 均为 0。已自查桌面首屏截图。

## 技能集合与首页内切换

2026-09-26：首页调整为技能集合介绍、目录、各 Skill 独立详情和通用使用说明。`site/skills.json` 驱动目录、数量、独立安装命令与调用示例；专属展示片段可选。

- 预览与正式网址构建均通过：16 个输出文件、27 项本地资源引用。正式构建只使用技能集合的标题与摘要，不再用 UI 样板分享图代表全站；尚未发布。
- 在内存追加一个无详情片段的测试 Skill，确认自动生成第二个条目和独立安装区、目录数量变为 2、无重复 HTML id，原 PC 样板仍只有一套。测试数据未写入实际清单。
- Chromium 1440×1000、390×844、320×740：集合首页、目录锚点与独立详情可用，无横向溢出，桌面与手机截图已检查。
- PC 的研究刊物、精密终端、现代账本均可在 Skill 区域内切换，主页面不跳转；iframe 保持 1440px 的 PC 画布。实际打开并关闭持仓详情，按钮键盘操作及原型内部方向键切换通过。
- 安装命令、调用示例实际复制通过；各安装区反馈独立。无 JavaScript 时目录与技能正文仍可读，PC 提供截图与原样板链接。
- 所有本页锚点有效、HTML id 唯一；本轮浏览器 Errors 0 / Warnings 0 / HTTP 4xx–5xx 0。

## 安装入口前置

2026-09-26：首页首屏直接显示仓库安装命令；每个技能目录条目直接显示单项安装命令，两者都有复制按钮。

- 1440×900、390×844、320×740：不滚动即可完整看到首页命令与复制按钮，无横向溢出；桌面和手机截图已检查。
- 实际读取剪贴板，首页命令为 `npx skills add luoqaq/luo-skills --agent codex -g`，目录命令包含 `--skill task-ui-prototype`，复制状态在各自区域显示。
- JavaScript 禁用时仍显示可手动复制的命令；浏览器脚本错误为 0。未执行安装或新增外部依赖。

## Agent 安装优先

2026-09-26：首屏、技能目录、技能详情及中英文 README 改为优先让 Agent 安装，终端命令保留为折叠备选。

- 1440×900、390×844、320×740：Agent 安装请求复制按钮在首屏内，终端命令默认折叠，无横向溢出；桌面与手机截图已检查。
- 实际复制的首页请求包含仓库地址、单项直接安装/多项先选择、当前 Agent 个人级范围、保留 memory/ 和报告结果。目录请求明确只安装对应 Skill。
- 展开终端备选后，复制命令准确；复制反馈明确提示粘贴给 Agent。浏览器脚本错误为 0。本轮未实际安装技能。
